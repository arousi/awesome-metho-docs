Diagrams
========

ERD, sequence, state, and architecture diagrams for the example project. Keep
PlantUML sources here (or under `ERD/` for entity-relationship diagrams).

Note: most LLMs get diagram logic wrong. A generated diagram is a prototype -
go over each one and assert its correctness before you rely on it.
