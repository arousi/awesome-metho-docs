Full Database Schema
====================

The consolidated, project-wide database schema - one place to see every entity
and its relationships together. The authoritative attribute-level detail for
each entity lives in its `../modules/<Module>/Entities.md` (DDT + PlantUML); the
cross-module entity registry is `Entities.md`. Keep this consolidated view in
sync with those as the design stabilizes.

## Overview

<Describe the schema: the main entities, how they group into modules, and the
key relationships.>

## Consolidated model

<Full DDL, or a single aggregate ERD / PlantUML covering every entity. Assert any
generated diagram before relying on it.>
